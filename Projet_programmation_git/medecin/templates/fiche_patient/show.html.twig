{% extends 'base.html.twig' %}

{% block title %}FichePatient{% endblock %}

{% block body %}
    <h1>FichePatient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_patient.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ fiche_patient.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_patient_index') }}">back to list</a>

    <a href="{{ path('fiche_patient_edit', {'id': fiche_patient.id}) }}">edit</a>

    {{ include('fiche_patient/_delete_form.html.twig') }}
{% endblock %}
