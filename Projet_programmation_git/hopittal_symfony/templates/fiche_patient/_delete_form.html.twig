<form method="post" action="{{ path('fiche_patient_delete', {'id': fiche_patient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_patient.id) }}">
    <button class="btn">Delete</button>
</form>
