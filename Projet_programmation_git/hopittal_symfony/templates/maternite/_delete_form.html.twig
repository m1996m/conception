<form method="post" action="{{ path('maternite_delete', {'id': maternite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maternite.id) }}">
    <button class="btn">Delete</button>
</form>
